Add functions those simply add, subtract, multiply and divide.
Create three variables for a number, an operator and another number.
Create an operator function that evaluates the above functions.
Create buttons for every button in calculator including = operator in HTML.
Add event listeners for the buttons from 0 to 9 and use array of operators to input operators.
Create a backspace and clear buttons.
Enable keyboard functionality by adding key events.

